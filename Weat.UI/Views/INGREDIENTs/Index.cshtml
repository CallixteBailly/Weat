@model IEnumerable<Weat.Entities.DataModel.INGREDIENT>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.URLIMAGE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NAMEINGREDIENT)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TYPEINGREDIENT.CAPTION)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.URLIMAGE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NAMEINGREDIENT)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TYPEINGREDIENT.CAPTION)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDINGREDIENT }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDINGREDIENT }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDINGREDIENT })
        </td>
    </tr>
}

</table>
